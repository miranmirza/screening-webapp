<%= link_to "Add candidate", new_candidate_path if current_user.admin? %></br>
Pending screening
<% @pending_candidates.each do |candidate| %>
  <%= link_to display_candidate(candidate.firstname, candidate.lastname, candidate.city), candidate_path(candidate) %>
  skype: <%= candidate.skype %>
  </br>
  <%= progress(candidate.status) %>
  </br>
<% end %>
</br>
</br>
Completed screening
<% @approved_candidates.each do |candidate| %>
  <%= candidate.firstname %>
  <%= candidate.lastname %>
  from <%= candidate.city %>
  skype: <%= candidate.skype %>
  </br>
  <%= progress(candidate.status) %>
<% end %>
